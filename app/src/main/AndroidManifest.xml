<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.controla_peso" >

    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="21" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/canva_boi"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/canva_boi"
        android:supportsRtl="true"
        android:theme="@style/Theme.Controla_Peso" >

        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CadastroActivit"
            android:label="Cadastro de pesagem" />
        <activity
            android:name=".PesquisaActivit"
            android:label="Pesagens Cadastradas" />
        <activity
            android:name=".CadastroBovinoActivit"
            android:label="Cadastrar bovino" />
        <activity
            android:name=".PesquisaCadBovinoActivity"
            android:label="Bovinos cadastrados" />
        <activity
            android:name=".ImagemTelaCheiaActivit"
            android:label="Imagem expandida" />
        <activity
            android:name=".SincronizarActivit"
            android:label="Sincronizar" />

        <activity
            android:name=".ImagemActivit"
            android:label="teste" />

        <activity
            android:name=".TotalizadorActivit"
            android:label="Totalizador" />

        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".MainActivity" />
    </application>

</manifest>